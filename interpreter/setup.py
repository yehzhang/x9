#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='x9-interpreter',
    version='0.1',
    author='Simon Zhang',
    license='MIT',
    packages=find_packages(),
    install_requires=[],
)
